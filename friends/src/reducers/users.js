import {
    
} from '../actions';

const initialState = {
    user: null,
    error: null
};

export const usersReducer = (state = initialState, action) => {
    switch (action.type) {

        default:
            return state;
    }
};
